services:
  app:
    entrypoint:
      - sleep
      - infinity
    image: adoptopenjdk/openjdk11:alpine
    init: true
    volumes:
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock